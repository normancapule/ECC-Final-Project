<% title "Edit Story" %>

<%= render 'form' %>

<p>
  <%= link_to "Show", project_story_path(@project, @story) %> |
  <%= link_to "Back to Release [#{@story.release.name}] Page", project_release_path(@project, @story.release ) %>
</p>
